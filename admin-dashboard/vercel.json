{
    "framework": "nextjs",
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/api/audio-proxy/:path*",
            "destination": "https://audio.iskcondesiretree.com/:path*"
        }
    ]
}